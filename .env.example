# Example Environment Variables for rag-eval project

# --- Core API Settings ---
# Used by app/config/settings.py for API authentication
# Generate a secure key for production
# RAG_EVAL_API_KEY="YOUR_SECURE_API_KEY_HERE"

# --- Redis Settings (for Celery results/broker) ---
# Used by app/celery.py
# CELERY_BROKER_URL="redis://localhost:6379/0"
# CELERY_RESULT_BACKEND="redis://localhost:6379/1"

# --- BuildDataset Feature Settings ---
# Used by app/build_dataset/config.py

# Required for fetching academic papers
SERPAPI_API_KEY=""

# Required for storing dataset artifacts
GCS_BUCKET_NAME=""
# Optional: Specify GCS Project ID if not using default ADC project
# GCS_PROJECT="your-gcp-project-id"
# Optional: Specify path to GCS service account JSON key file for auth
# GCS_SERVICE_ACCOUNT_FILE="/path/to/your/service-account-key.json"

# Optional: URL for the Playwright MCP server if used for web scraping
# Defaults to http://localhost:8070/extract if not set
# PLAYWRIGHT_MCP_URL="http://your-mcp-server-url:port/extract"

# --- Other Potential Settings ---
# Add other environment variables as needed for different features
